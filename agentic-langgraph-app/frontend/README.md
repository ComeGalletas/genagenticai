# Frontend (React + Vite)

## 1) Setup

```bash
cd frontend
npm install
```

## 2) Run

```bash
npm run dev
```

By default, the app calls `http://localhost:8000/api/chat`.
Use `VITE_API_BASE_URL` in a `.env` file if needed.
